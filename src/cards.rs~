pub enum Value {
    Ace(bool),
    Number(u32),
    Face(FaceType),
    Joker,
}

pub enum FaceType {
    Jack,
    Queen,
    King,
}

pub struct Card {
    value: Value,
    suit: Suit,
    state: State,
}
